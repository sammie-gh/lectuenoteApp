<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hammer.lecturenoteapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".Utils.Offline"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginLectures" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".UploadActivity" />
        <activity android:name=".MainAdminActivity" />
        <activity android:name=".BatchUpload" />
        <activity android:name=".AddpdfList" />
        <activity android:name=".ClientFolder.PdfViewList" />
        <activity android:name=".ClientFolder.DownloadPdfActivity" />
        <activity android:name=".StudentRecordsActivity" />
        <activity android:name=".ViewDocumentListActivity" />
        <activity android:name=".DownloadActivity"></activity>
    </application>

</manifest>